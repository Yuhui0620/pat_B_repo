/*
#include <stdio.h>
#include <math.h>

int main(void) {
	int n;
	int a, b;
	scanf("%d", &n);
	double maxR = -1;
	for (int i = 0; i < n; i++) {
		scanf("%d %d", &a, &b);
		double r = sqrt(a*a + b*b);
		if (r > maxR)
			maxR = r;
	}
	printf("%.2lf", maxR);

	return 0;
}
*/